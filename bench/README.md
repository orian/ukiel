# Macro perf harness (plan 30)

Volume-scale benchmarks on real datasets (ClickBench `hits`, Bluesky ndjson),
run manually against the docker-compose stack via the `bench` binary in
`ukiel-e2e`. Datasets and results live under `bench/datasets/` and
`bench/results/` (both gitignored).

**Full runbook, dataset provenance, resource requirements, and recorded
baselines:** [`docs/notes/2026-07-08-macro-perf.md`](../docs/notes/2026-07-08-macro-perf.md).
